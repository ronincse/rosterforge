{
  "name": "@rosterforge/repository",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@rosterforge/battlescribe-data": "workspace:*",
    "@rosterforge/foundation": "workspace:*"
  }
}
