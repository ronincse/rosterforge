# Shared reference engine correctness — 2026-09-19

Baseline: `2fdfbee8b84078c9f10313382ea5c1f150fa2c12`, pilot branch.
Print layout is unchanged; owner visual acceptance remains pending.

## A — explicit selector anchor

Demonstrated RF selector defect: parsing retained explicitSelf but entries
traversal ignored it. The editor exposes separate Affect Self/Affect Scope,
Affect child Selections and Recursive switches: [immutable primary evidence](https://github.com/giloushaker/nr-editor/blob/a347b0d96f71e40fffb30d8994897d276638c43f/components/catalogue/right_panel/fields/ComplexQuery.vue).
Scope discovery's Include self is a different control. No new external New
Recruit runtime comparison was performed. The earlier Lord of Contagion
observation does not prove anchor exclusion: its weapon profile filter excludes
its Unit profile independently.

Scope=model resolves the enhancement's nearest model bearer, the Captain.
The durable/materialized enhancement remains its child. Explicit self adds the
anchor alongside entries/recursive; category matching still filters each reached
occurrence, then profiles.Unit selects the declared profile type. No implicit
self, typed-query, force, association or category semantics were broadened.
A zero-entry-step retained group is not the anchor. Malformed relevant profile
selectors retain aggregate incompleteness rather than a confident exclusion.

Unchanged source: Captain 6c10-5b51-5bc0-8d2c, profile c1ba-2289-ba1-9cb8,
Sv 450-a17e-9d5e-29da, Artificer Armour e57e-d55a-be75-2205, set 2+,
scope=model, self.entries.recursive.9cfd-1c32-585f-7d5c.profiles.Unit.
Shared evaluation now applies the operation: **Sv 2+, base 3+**. The obsolete
print-only warning is retired. Historical print observations remain historical.

The bounded A-snapshot survey counted 2,072 entries selectors, 2,058 with explicit
self; 64 model-scoped selectors end in profiles.Unit. Fictional import-to-reference
matching-anchor cases failed before repair (2+ expected, 3+ observed). Coverage
includes category boundaries, descendants, absent self, independent duplicates,
removal, undo/redo, saved-source restoration and transparent-group routing.
Existing category/annotation assertions encoded the disproved exclusion; two
authored annotation appends now both survive. The frozen Dark Angels document
passes with Captain 2+ and unchanged 2,000 points. Final measurements appear below.


## B — occurrence-specific rule names

The old rule inspection evaluated visibility only: the frozen Impulsor exposed
append-name operands as unevaluated operations. A probe on the isolated baseline
`2fdfbee` engine, using the new actual-army label assertions, failed for both
Impulsor effective names and the selected unqualified FNP 5+ record (1 test failed,
1 intentionally unselected StarCraft case; 3.11s). The tracked stronger final
actual-army test passes, including Captain 2+. Log: `baseline-rule-name-failure.log`. The new name report reuses the
profile lexical step evaluator and existing condition/group applicability; it is
not a print concatenator. Names and visibility retain independent completeness.

Within one source carrier, direct modifiers run in authored order, then recursive
modifier groups in the same established order as profile text evaluation.
Supported owned `set` and `append` preserve literal operands and authored `join`
(including empty or nonbreaking space); absent join means one space. Definitely
inactive operations do not write. Missing targets/operands, unknown conditions,
unsupported groups/repeats/routing and missing/ambiguous occurrence identity stay
incomplete. A later set can recover the displayed value without erasing earlier
uncertainty. Source-only conditional/grouped operations cannot assert occurrence
applicability. Annotation/format-rule execution is outside this boundary.

A static link name is the existing materialized source base. A definition-only
operation is supported when no explicit link name competes. Two possibly active
writing carriers, or a modified definition plus any explicit static link name,
remain unresolved because their dynamic precedence is unestablished. Candidate
values and original modifier/carrier provenance remain available; equal candidates
do not establish precedence. Original IDs, paths and persisted labels are unchanged.

Frozen results, through the shared runtime:

| Selected source | Operation | Effective label |
| --- | --- | --- |
| Impulsor `bfb1-7512-e1a3-9fa2`, link `58e0-31f7-43d3-b1ef` | append `D3` to name | Deadly Demise D3 |
| Impulsor link `31bc-9145-ac64-454e` | append `6` to name | Firing Deck 6 |
| Selected Artificer Armour rule link | append `5+`, authored nonbreaking-space join | Feel No Pain 5+ |

These reports are complete with applied provenance, not displayed candidate
operands. No dice/parameter parser infers values from explanatory X. Selected
cards, prose lookup, print/PDF and standalone HTML use the same names. Original
source labels remain lookup aliases to the same record; collisions stay ambiguous.
Parameterized keyword matching does not silently reuse a different effective label.
Identical bodies may be shared while effective labels and owner mappings remain
separate. Applied operations lose only their obsolete unevaluated warning.

Fictional XML import/materialization tests cover direct and linked definitions,
distinct operands on two links, explicit static override uncertainty, ordered
noncommutative operations, true/false/unknown conditions, malformed targets,
unsupported groups/repeats, missing/ambiguous identities, repeated evaluation,
independent duplicates, undo/redo, and two saved-source reconstruction passes.
Existing hidden/unresolved-rule tests remain. A real App import/create/add test
renders encoded dangerous-looking labels as inert text; standalone HTML escapes
labels once. No additional XML decoding or imported markup execution occurs.

## Frozen inputs and runtime acceptance

40k A remains `04c62fcd041b3808c39d5c46fd677c704027b979`; B remains
`5b261ec423d5d017bb733c4f3c0a760b085d5ca5`; StarCraft remains
`99261754e0449bbaaa04e6890e1625b144f9ece1`. Both 40k eight-file closure manifests
were hash-verified. All eight embedded source payloads in the recorded draft
match A byte-for-byte. Original draft SHA-256 remains
`9f0f9b94152102db6fddb378f34eaea9855874dc0b12ceb9ebfa0441cdc4937c`.
No third-party source data or personal army is committed or sent to reviewers.

The actual recorded draft was copied into isolated origin `http://127.0.0.1:5293/`
using the existing draft decoder/storage adapter, then opened and edited through
normal application controls. This is disposable adapter seeding, not a claim of
browser file-import acceptance. The original has 17 root selections: three setup
entries and 14 army units, totaling 2,000 points. Tested normally:

- Selected Captain: 100 points, Sv 2+ from base 3+, Set by Artificer Armour and
  complete Feel No Pain 5+. Removing its actual selected enhancement gives
  Captain 80 points / army 1,980 / Sv 3+ and removes that rule.
- Undo restores 2,000/2+; redo restores 1,980/3+. Adding Armour through the normal
  configuration control restores the effect. No renderer hardcoded value.
- Duplicate Captain gives 15 units/2,100. Removing Armour from one leaves the
  duplicate's 2+/5+ intact; undo returns the original 14-unit/2,000 configuration.
- Reload and normal saved-army Open preserve 14 units/2,000, Captain 2+ and one
  complete FNP 5+ rule. Explicit Update saved draft succeeds. Original sources,
  source pins, budgets and unrelated saved armies remain untouched.
- Impulsor card shows complete D3/6. Clicking original prose "Firing Deck" opens
  the effective Firing Deck 6 reference. Both print presets show the same results.
  Unrelated Lieutenant remains Sv 3+; Supporting Lethal Hits remains on U6.

The isolated preview remains running for owner testing; existing server origins
and tabs were preserved. No claim of a new phone/desktop matrix or new New Recruit
runtime comparison is made for this batch.

## Paper evidence and independent review

Only the affected Dark Angels Letter samples were regenerated through the actual
shared document pipeline. Compact is 22 pages; Unit sheets 27. Every final page
was visually inspected using the PDF skill: isolated native reviewer inspected
all 22 Compact pages, lead inspected all 27 Sheets pages. No clipping, overlap,
truncation or broken glyphs found. Existing short Sheets tails (14/17/19) and
Compact paragraph continuations remain; no density/layout policy was changed.
Owner visual acceptance remains pending; no physical-printer claim.

Captain is Compact p2 / Sheets p2-3; Impulsor Compact p14 / Sheets p19; full
parameterized glossary prose and occurrence mappings Compact p19 / Sheets p24.
R32/R33 belong to U14 (Impulsor); selected FNP R6 belongs to U4/Armour, while
source-only generic FNP R8 remains a separate explanation. All 47 scope records
remain. Existing profile/member and separate-unit cost assertions pass.

New evidence only:
`C:/Users/stone/.codex/visualizations/2026/09/12/01a09368-161b-7c60-a3c6-b0afa598b7de/engine-correctness/`.
Final PDFs are `output/dark-angels-compact-Letter.pdf` and
`output/dark-angels-sheets-Letter.pdf`; matching HTML/facts, `pdf-settings.json`
and all page PNGs are beside them. Edge 154.0.4258.18, offline network-blocked
HTML, Letter portrait, scale 1, CSS 14mm margins, backgrounds/headers disabled.
Original and revised owner-review artifacts are intact. Final guard changes
regenerated identical PDF bytes/page counts; subsequent changes are tests/docs.

Codex remained implementer/integration owner. The isolated native reviewer
challenged routing, group ordering, uncertainty and glossary boundaries; zero-step
group exclusion, malformed profile-selector completeness, missing-field group
ordering and recovered-name qualification were addressed. A scoped tools-disabled,
safe-mode/plan Claude Code 2.1.240 review completed using **Claude Opus 5** on a
curated source/diff/fictional-test packet. No personal army, dataset or secrets
were transferred. Its earlier acknowledgement-only response is not counted as a
review. The substantive review identified the static-link/dynamic-definition
precedence gap, now guarded/tested, and improved unresolved-operation diagnostics.
Claims that owned `affects` was accepted or qualification was not rendered were
checked against code and rejected: existing attribute checks and actual rendering
cover them. Its performance concern was bounded by existing immutable context/
roster WeakMap caches; no measured regression justified a separate optimization.

## Verification and remaining boundaries

Final lint, typecheck, build and whitespace gates pass. Ordinary run:
**1,023 passed / 34 optional skips**, 97 passed / 8 skipped files (18.95s).
Configured run: **1,057 passed / zero skips**, all 105 files (53.26s), maxWorkers=4.
The existing large-bundle build advisory remains; no gate was weakened.
Exact commit/publication/CI receipt is in the handoff and completion response.
The normal optional skips are not corpus coverage: the configured run supplies
`ROSTERFORGE_BSDATA_JSON_DIR`, `ROSTERFORGE_CORRECTNESS_SNAPSHOTS`,
`ROSTERFORGE_STARCRAFT_PILOT_DIR` and `ROSTERFORGE_PRINT_DARK_ANGELS`.
This runs both 40k frozen snapshots, StarCraft hashes and the actual army projection.
Supporting, pricing, budgets/queries, initialization, persistence/recovery,
reference roles/layout, Marines numeric scope/reinforcement, faction/supply,
typed-constraint uncertainty and renderer timing regressions remain green.

This is bounded reference correctness, not full BattleScribe/game support.
Unestablished cross-carrier name precedence, broader typed-query/anchor discovery,
unsupported routed rule names, repeats/groups, annotations and imported formatting
remain qualified. Malformed profile selectors mark aggregate incompleteness;
that is not a new per-field unknown-value model. Existing source revision mismatch,
orphan costs, association, dynamic/zero-default budgets and freshness limitations
are unchanged. No further StarCraft or print-layout checkpoint is authorized here.
